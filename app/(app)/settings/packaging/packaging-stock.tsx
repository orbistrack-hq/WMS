// Moved: packaging stock is now a single central pool managed at
// Intake → Packaging (app/(app)/inventory/packaging/). This per-site stock card
// was removed in migration 0047. Kept as an empty module because the workspace
// can't delete the file.
export {}
